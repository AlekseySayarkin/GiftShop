plugins {
    id 'java'
}

group 'com.epam.esm'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile project(":model")
    compile project(":persistence")
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
